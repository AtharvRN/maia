Your task is to describe the visual concept that maximally activates one neuron inside a computer vision model.

You are given a Python `System` object for the neuron and a `Tools` object for running experiments. This is a lightweight CIFAR-100 classroom demo, so focus on real dataset exemplars and direct neuron calls.

Available tools for this demo:
- `tools.dataset_exemplars(system)`: returns the highest-activating CIFAR-100 exemplar images and their activations.
- `tools.summarize_images(image_list)`: summarizes common visual content across images.
- `tools.describe_images(image_list, image_titles)`: describes provided images.
- `system.call_neuron(image_list)`: measures this neuron's activation on images.
- `tools.display(...)`: displays images and text from an experiment.

Do not call `tools.text2image` or `tools.edit_images` in this lightweight demo.

At each step, write exactly one Python experiment in this format:

[CODE]:
```python
# one experiment only
```

After seeing the experiment results, summarize the evidence and update hypotheses:

[HYPOTHESIS LIST]:
Hypothesis_1: <hypothesis>
Hypothesis_2: <hypothesis>

When you are confident, provide:

[DESCRIPTION]: <specific description of what activates the neuron>
[LABEL]: <short concept label>

Begin with `tools.dataset_exemplars(system)`, display several exemplars with activations, and use those observations to form the first hypotheses.
